/** Re-export so callers can depend on `utils/newsRegion` without importing config directly. */
export {
  NEWS_REGION_OPTIONS,
  DEFAULT_NEWS_REGION_CODE,
  normalizeNewsRegionSelection,
  normalizeNewsRegion,
} from '../config/newsRegions';
